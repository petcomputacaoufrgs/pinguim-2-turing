import React, { useState } from 'react';
import * as wasm from "turing-wasm";

interface EncodingProps {
    // Define any props for the component if needed
  }
  
  const Encoding: React.FC<EncodingProps> = (props) => {
    return (
      <div>
        {/* Component content goes here */}
      </div>
    );
  };
  
  export default Encoding;


