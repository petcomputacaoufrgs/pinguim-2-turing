//import init, { add } from "turing-wasm";

import React from 'react';
import TMPage from './TMPage';

const App: React.FC = () => {
  return (
    <div className="App">
      <TMPage />
    </div>
  );
};

export default App;
